#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'has_jwt_token'

if ENV['GEM_ENV'] == 'test'
  require_relative '../spec/fixtures/dummy_user'
  require_relative '../spec/fixtures/dummy_login_service'
end

require 'pry'
Pry.start
